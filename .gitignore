node_modules
.next
out
coverage
playwright-report
test-results

# Written at repo root by the CI dependency advisory step and uploaded as a
# build artifact. The committed evidence copy lives at
# docs/production/dependency-audit-result.json.
/dependency-audit-result.json
.env*
.DS_Store
*.tsbuildinfo
.worktrees/

# Auto-generated by Next.js and rewritten between dev and build modes
# ("./.next/dev/types" vs "./.next/types"), which produced recurring phantom
# diffs. Regenerated automatically by `next build`; typecheck passes without it.
next-env.d.ts

# Regenerated on every `npm run security:scan-skills` run. The reviewed,
# committed artifact is docs/security/skillspector-baseline.yaml.
docs/security/skillspector-scan-result.json
